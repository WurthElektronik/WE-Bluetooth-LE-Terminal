import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eisos.proteusconnect',
  appName: 'Proteus Connect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
